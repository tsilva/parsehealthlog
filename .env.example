# API key (required - kept in environment for security)
OPENROUTER_API_KEY=<your API key>

# All other settings should be in profiles/*.yaml
# See profiles/_template.yaml for available options
# Run: uv run python main.py --list-profiles
